server {
    listen 80;
    server_name localhost;

    location = / {
        alias /usr/share/nginx/html/docs/home/index.html;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ =404;
    }
}